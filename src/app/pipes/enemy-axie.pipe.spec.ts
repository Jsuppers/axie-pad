import { EnemyAxiePipe } from './enemy-axie.pipe';

describe('EnemyAxiePipe', () => {
  it('create an instance', () => {
    const pipe = new EnemyAxiePipe();
    expect(pipe).toBeTruthy();
  });
});
