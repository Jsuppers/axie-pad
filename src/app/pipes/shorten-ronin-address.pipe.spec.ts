import { ShortenRoninAddressPipe } from './shorten-ronin-address.pipe';

describe('ShortenRoninAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortenRoninAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
